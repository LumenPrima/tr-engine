-- Rollback: This migration cannot be rolled back cleanly.
-- The surrogate IDs were dropped and cannot be recreated with the same values.
-- To rollback, restore from a backup taken before applying this migration.

-- This file intentionally does nothing to prevent data corruption.
-- If you need to rollback, restore from backup.

SELECT 'WARNING: Migration 005 cannot be rolled back. Restore from backup if needed.' AS message;
